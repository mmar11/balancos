"use client";
import React from "react";

const VerAnaliseButton = () => {
  return (
    <div>
      <button
        onClick={() => {
          console.log("click");
        }}
      >
        ANALISE
      </button>
    </div>
  );
};

export default VerAnaliseButton;
